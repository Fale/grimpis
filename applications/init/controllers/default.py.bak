# coding: utf8
# try something like

@auth.requires_login()
def index():
    auth.add_membership(2, 1)
    return dict()

def user():
    return dict(form = auth())

#@auth.requires_permission('admin')
def data():
    return dict(form = crud())
